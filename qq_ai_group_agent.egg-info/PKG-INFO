Metadata-Version: 2.4
Name: qq-ai-group-agent
Version: 0.1.0
Summary: A local QQ/NapCat group summarizer and AI agent
Requires-Python: >=3.10
Description-Content-Type: text/markdown
Requires-Dist: fastapi<0.116,>=0.110
Requires-Dist: uvicorn[standard]<0.35,>=0.27
Requires-Dist: httpx<0.29,>=0.27
Requires-Dist: jinja2<4,>=3.1
Requires-Dist: keyring<26,>=24
Requires-Dist: python-multipart<0.1,>=0.0.9
Requires-Dist: pypdf<7,>=4
Requires-Dist: PyMuPDF<1.27,>=1.23
Requires-Dist: yt-dlp[default]<2027,>=2025.1
Provides-Extra: dev
Requires-Dist: pytest<9,>=8; extra == "dev"
Requires-Dist: pytest-asyncio<0.26,>=0.23; extra == "dev"

# QQ 群 AI 代理（本机版）

这是一个仅在 Windows 本机运行的 QQ 群聊天 AI 代理。它通过 QQNT + NapCat 的 OneBot 反向 WebSocket 接收群消息，把**已在控制台明确启用**的群聊/私聊事件按顺序交给一个 OpenAI 兼容 API；模型可以在当前会话的独立工作目录中处理文件、执行命令、发送群文件或撤回本应用已发送的消息。

> 这是 NapCat/QQNT 的非官方接入方案。请自行评估账号稳定性、QQ 平台规则、群成员知情同意以及把群聊内容交给第三方 API 的隐私风险。

## 安全与行为边界

- 本项目**不收集、不保存 QQ 密码**。登录应在 QQNT/NapCat 中通过官方扫码流程完成。
- 控制台只监听 `127.0.0.1`，不应通过端口映射、反向代理或公网隧道暴露给其他人。
- 新发现的群默认关闭；只有你在本机控制台中启用的群会采集消息并向模型 API 上传内容。
- 每次启用群时，服务会读取最近最多 200 条、且文本最多 50,000 字的历史。最新约 50,000 字群聊原文会在每次模型请求中直接提供；它不会被立刻压缩。只有自然被挤出这段原文窗口的更早事件才进入滚动摘要。
- 群摘要是滚动的连续历史状态：每次归档旧事件时，模型都会带上上一份累计摘要，保留仍相关的结论、上下文和未解决事项后再融合旧内容。控制台群卡片显示的是这份“历史摘要”；“最近处理记录”只是当时的处理快照。
- 机器人可以自主参与有价值的讨论；除了实时 `@`，成员使用 QQ 的“回复”功能回复本应用发出的消息也会被识别为直接互动并要求机器人自然接话。对“有人吗 / 在吗 / 是人的发 1”这类明确等待在场者回应的实时消息，也会触发一条简短自然回复。普通文本中伪造的回复标记不会触发它。
- 若同一群聊/私聊的主 Agent 正在处理上一轮，期间新到的成员消息会启动一个独立、无工具的快速辅助回复：它只收到当前主任务的有界阶段/工具/本会话上下文快照和新消息，并只会自然说明“仍在处理、已收到”。辅助回复不改变主队列、摘要、记忆或工具决策；其 QQ 发送有独立幂等记录，NapCat 超时后不会重发。
- 图片会保存到本机媒体目录；默认预算为 20 GiB。QQ CDN 图片 URL 过期时，服务会向 NapCat 请求 `get_image`/`get_file` 刷新 URL 或读取其经过验证的本地图片缓存。达到上限时只淘汰最旧原图，不删除消息记录、摘要或工具审计记录。
- 模型工具固定在当前会话内：可发送文字/文件、撤回本应用已经发送的消息，生成图片、将 Markdown 渲染成长图片，列出/下载群文件，编辑当前会话工作目录并执行本地命令；另有 `Builtin_Websearch`（Google 搜索）、`Builtin_querymessage`（仅当前会话前文）、`Builtin_querymemory` 和 `Builtin_patch`（抓取公开链接）。执行命令时会先发送“正在执行指令：……”，仅支持使用本机 `yt-dlp` 下载 Bilibili 视频，默认最高 720P。模型无法改变目标会话或读取其他会话工作目录。

## 前置条件

- Windows 10/11，Python 3.10 或更高版本（当前 yt-dlp 版本要求 Python 3.10+）。
- 可正常运行的 QQNT 与 NapCat；建议使用专门的 QQ 账号，而不是主账号。
- 若要下载 Bilibili 或 YouTube 视频、或分析视频，请安装 `yt-dlp` 和 `ffmpeg`；YouTube 完整解析还需要 Node.js 22+（启动脚本会自动发现并传给 yt-dlp）。服务除了读取自身 PATH，还会自动查找 `C:\\ffmpeg*\\bin\\ffmpeg.exe`；如果服务由任务计划或隐藏启动导致 PATH 不同，可设置 `FFMPEG_PATH` 为 `ffmpeg.exe` 完整路径后重启服务。运行数据目录中的 `bilibili-cookies.txt` 和可选的 `youtube-cookies.txt` 会被分别作为 Netscape cookie 文件使用，服务不会把 cookie 内容发给 QQ。两个视频工具默认选择不超过 720P 的格式，并在发送前转码为 QQ 兼容的视频卡片；如果站点返回登录、风控或格式错误，工具会保留 yt-dlp 的诊断供模型如实反馈。
- Markdown 图片工具使用本机 Microsoft Edge 与 `D:\\项目\\MarkFlow\\vendor\\markdown` 的 `marked`、`highlight.js`、KaTeX 资源，效果与 MarkFlow 的右侧预览一致。QQ 纯文本只保留给简短、简单且无需排版的对话；题解、长答案、代码、公式、表格和多步骤说明会自动调用 `Builtin_render_markdown_image` 发送渲染后的长图，不需要成员额外说“渲染”。服务端也会拒绝把这类内容作为普通文本发送。仅当当前成员明确索要 Markdown 源码时才会直接发送原始 Markdown。若 MarkFlow 不在默认路径，可设置 `QQ_AI_MARKFLOW_ROOT`；若 Edge 不在标准位置，可设置 `QQ_AI_MARKDOWN_RENDER_BROWSER`。
- 一个 OpenAI-compatible 服务地址、模型名和 API Key。可选择 Chat Completions 或原生 Responses 端点；若需要识图，目标模型也必须兼容图片内容段。

NapCat 的安装、版本兼容性和 QQ 登录流程以其官方文档为准：

- [NapCat 安装与启动](https://napneko.github.io/guide/install)
- [NapCat WebUI / OneBot 网络配置](https://napneko.github.io/config/basic)
- [NapCat 事件兼容情况](https://napneko.github.io/develop/event)

## 安装并启动

在项目根目录的 PowerShell 中执行：

```powershell
py -3.12 -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install --upgrade pip
python -m pip install -e ".[dev]"
.\scripts\start.ps1
```

如果 Windows PowerShell 提示“running scripts is disabled”，不要修改全局执行策略，改用项目自带的包装器（它只对当前进程临时放行）：

```powershell
.\scripts\start.cmd
```

也可以直接执行：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\start.ps1
```

如果 8765 已经有本代理在运行，启动器会显示当前健康状态并直接返回；若端口被其他程序占用，会显示占用进程和可用的 `-Port` 替代方案。

首次运行后，打开 [http://127.0.0.1:8765](http://127.0.0.1:8765)。在“模型与连接”中填写：

1. 选择模型 API 的请求端点模式，再填写 URL、模型名和 API Key。三个模式的含义如下：

   | 模式 | URL 应填写 | 实际请求 | 请求格式 |
   | --- | --- | --- | --- |
   | `base` | 供应商给出的**完整请求 URL** | 原样 `POST` 该 URL | Chat Completions 兼容格式 |
   | `completions`（默认） | API 根地址，例如 `https://api.example.com/v1` | 根地址 + `/chat/completions` | Chat Completions 格式 |
   | `responses` | API 根地址，例如 `https://api.openai.com/v1` | 根地址 + `/responses` | 原生 OpenAI Responses 格式 |

   API Key 会以明文持久化到运行数据目录的 `api-key.json`，永不在网页中回显，留空代表不修改它。默认运行数据目录是 `%LOCALAPPDATA%\QQAIGroupAgent\data`；程序会尽力限制为当前 Windows 用户可读写，仍请不要上传、同步或分享此文件。
2. OneBot Token；它必须和 NapCat 反向 WebSocket 客户端的 Token 一致。它保存到 Windows 凭据管理器，且永不回显。
3. 设置单次模型请求超时（秒），可选地启用图片视觉、设置媒体容量，以及决定是否把 `reasoning_effort` 透传给供应商。
4. 等 NapCat 连上后，在“发现的群”中明确启用所需群。停用会立即停止新采集与 API 上传，但不会删除历史数据。

如果 Gemini/Google 代理明确返回敏感内容安全拒绝，服务会先记录失败并回退，然后自动用仅事件类型、时间、字符数和媒体计数的元数据重算；安全降级请求不会上传原文、图片、昵称、链接或旧 QQ 动作。这样可以继续维护不含敏感细节的摘要，但涉及原文的长期记忆会暂时保留游标，待换用可处理该内容的模型后再重试。

实时群聊/私聊消息默认允许 Agent 像真人一样自主判断是否参与：明确问题、追问、值得补充的内容和自然互动通常会主动回复；纯刷屏、重复、单个表情/数字或无关内容可以沉默。没有 @ 不是沉默的理由，但也不是每条消息都必须回复；历史归档轮仍然只更新内部摘要，不会补发旧消息。

每个群/私聊卡片都有“清空本群记忆与规则并重算历史”操作。它会物理删除该会话的结构化长期记忆、记忆证据/版本记录、滚动摘要及群级补充提示词，然后把已保存事件重新送入摘要与记忆流水线；原始消息、媒体、工具审计和机器人已发送记录保留。重算只进行归档/记忆处理，不会重复发送旧消息、撤回旧消息或重新执行旧工具。全局管理员 `rules.md` 不属于某个群，不会被该按钮清空。

## 会话工作目录、群文件与本地指令

每个已启用的群聊或私聊都有独立的 Agent 工作目录，默认是：

```text
D:\Workspace\<群号或私聊编号>
```

也可以通过环境变量 `QQ_AI_WORKSPACE_ROOT` 改变根目录。模型可使用 `list_workspace_files`、`read_workspace_file`、`write_workspace_file` 和 `execute_command` 编辑、编译、测试或处理文件；命令的当前目录固定为当前会话目录。`send_group_file` 会把工作区文件发送到当前群/私聊。

OneBot 的 `group_upload` 动态会进入当前会话原文，并带有文件 ID、名称和大小。视频消息也会进入原文并带有视频 ID/文件名；模型调用 `Builtin_video_understanding` 后，服务使用本机 ffmpeg 每 10 帧抽取截图，按每 300 KiB 分块发送给配置的视觉模型（Gemini 兼容接口），每块生成最多 20,000 字的详细总结，再递归合并所有分块总结后把最终视频理解结果返回主 Agent。模型可用 `Builtin_list_group_files` 查看当前群文件，用 `Builtin_download_group_file` 下载到当前会话工作目录后再读取、编辑或发送。Bilibili 请求会调用 `Builtin_bilibili_download`，YouTube 请求会调用 `Builtin_youtube_download`；YouTube 没有链接时可传 `query`（例如 `Sharks Official MV 144p`），两个工具默认使用不超过 720P 的最佳格式并在发送前转为 QQ 视频卡片；模型可以按用户要求传入 `format_selector`、`output_extension`、`audio_only` 或 `filename`。音乐请求会调用 `Builtin_music_download`：服务用 yt-dlp 下载音频、ffmpeg 转成单声道 MP3，再通过 OneBot `record` 消息段发送 QQ 语音，不会上传成群文件；支持群聊和私聊。

下载视频时，服务会在发送前用 ffmpeg 统一转换为 QQ 兼容的 H.264/AAC MP4（yuv420p、faststart），再发送原生视频卡片；通过 `send_group_file` 发送工作区视频也使用同一兼容转换流程。

音乐或工作区音频不会作为普通文件上传：服务会用 OneBot `record` 消息段发送，并把超过 50 秒的音频用 ffmpeg 自动切成每段不超过 50 秒的连续 QQ 语音。每段使用 `file:///` 本地 URI，交给 NapCat 转成 QQ 的语音格式。

`start.ps1` 默认绑定 `127.0.0.1:8765`。常用选项：

```powershell
# 本地开发热重载
.\scripts\start.ps1 -Reload

# 遇到闪退时保留错误窗口
.\scripts\start.ps1 -KeepOpenOnError

# 改为另一个本地端口
.\scripts\start.ps1 -Port 8877

# 明确指定运行数据目录（应放在健康的 NTFS 磁盘）
.\scripts\start.ps1 -DataDir 'C:\QQAIGroupAgent\data'

# 启动安装了 NapCat 的 QQNT，再启动本服务
.\scripts\start.ps1 -NapCatExecutable 'C:\Path\To\QQ.exe'
```

## 配置 NapCat 反向 WebSocket

在 NapCat WebUI 中，先通过二维码登录需要绑定的 QQ。然后前往网络配置，新建并启用一个 **WebSocket 客户端（反向 WS）**；不要使用正向 WS 服务端。

使用以下本机参数（应用默认端口为 8765）：

| NapCat 字段 | 值 |
| --- | --- |
| 类型 | WebSocket 客户端 / 反向 WS |
| URL | `ws://127.0.0.1:8765/onebot/v11` |
| Token | 自行生成的高强度随机字符串；与控制台中的 OneBot Token 相同 |
| 上报格式 | `array` |
| `reportSelfMessage` | `false` |
| 重连间隔 | `5000` ms（或你的偏好） |
| 心跳周期 | `30000` ms（或你的偏好） |

参考的 NapCat 配置形状如下；请优先使用 WebUI 保存，而不是手工覆盖已有配置：

```json5
{
  "network": {
    "websocketClients": [{
      "name": "qq-ai-group-agent",
      "enable": true,
      "url": "ws://127.0.0.1:8765/onebot/v11",
      "messagePostFormat": "array",
      "reportSelfMessage": false,
      "reconnectInterval": 5000,
      "token": "与本机控制台中的 OneBot Token 相同",
      "heartInterval": 30000
    }]
  }
}
```

保存并启用后，控制台顶部的 OneBot 状态应变成“已连接”。如果未连接，请检查：QQNT/NapCat 是否仍在运行、URL 是否是 `ws://127.0.0.1:8765/onebot/v11`、Token 是否完全相同，以及 Windows 防火墙或其他进程是否占用了端口。

## 思考深度与模型兼容性

全局和群级都可以选 `off`、`minimal`、`low`、`medium`、`high` 或 `xhigh`。群级“继承全局设置”会使用全局值。只有勾选“向 API 透传思考深度”时，该字段才会发送给模型服务：Chat Completions 模式使用 `reasoning_effort`，Responses 模式使用 `reasoning.effort`。供应商拒绝该字段时，服务会自动去掉该字段重试一次并在控制台显示告警。

OpenAI-compatible 提供商在工具调用、视觉输入或 `reasoning_effort` 的字段支持上可能不同。建议先在测试群中验证纯文本摘要，再启用图片和自动工具调用。

模型在一次处理内最多执行 16 个工具调用、最多进行 4 次有工具可用的 Agent 决策。工具按顺序执行；出现真正失败时，后续调用会被停止，完整的失败 JSON 会回传给模型用于最终答复/摘要，不能假装工具成功。服务可能对本地参数校验失败给出一次无副作用的安全修正机会。

## SQLite 启动错误与图片诊断

正常启动默认使用 `%LOCALAPPDATA%\QQAIGroupAgent\data\agent.sqlite3`，不会再把频繁写入的 SQLite 放在项目所在的 D 盘。直接运行 `uvicorn app.main:app` 也采用同一默认目录。启动时会打印实际运行数据路径；也可用 `start.ps1 -DataDir ...` 覆盖。数据目录应位于健康的 NTFS 磁盘，不建议使用已报告文件系统错误的 exFAT 盘。

如果 PowerShell 显示 `sqlite3.OperationalError: disk I/O error` 且位置在 `PRAGMA journal_mode = WAL`，新版服务会先安全降级为 `DELETE` 日志模式，并在控制台显示告警。若 WAL 和 DELETE 都无法启用，说明运行数据目录中的 `agent.sqlite3` 或其父目录不可写、被锁定或磁盘本身异常；先停止服务并复制原库备份，不要反复在损坏库上启动或执行写入。程序不会自动删除或覆盖原数据库。

若图片事件只显示 `[图片]`，先看群卡片/最近事件中的 `storage_error`。NapCat 给的 QQ CDN `url` 是短时地址；本服务会自动尝试刷新。若日志仍显示 NapCat 无法获取 rkey 或当前 QQ 版本的 PacketBackend 不支持，NapCat 本身无法提供图片资源，升级/等待与 QQNT 匹配的 NapCat 版本后再试。

## 在本机后台与 AI 对话和长期记忆

控制台顶部的“与 AI 对话”面板只接受本机管理员的输入。它适合告诉 AI 机器人应如何参与讨论、如何回答某类问题，或请它审阅一条希望长期保留的偏好。该对话会使用当前配置的模型 API，但不会向 QQ 群发送消息。

- AI 可以直接回答，而**不**写入长期记忆。
- AI 只有在判断你的请求适合长期保留时，才可通过唯一受限的记忆工具更新运行数据目录中的 `rules.md`；它不能借此修改 API、OneBot、群启用状态或其他工具边界。它也可以明确选择不写入。
- 面板会显示最近对话及只读的 `rules.md` 内容。每个后续群聊模型请求都会读取它，但不可变的服务安全边界仍优先。要修改规则，请在对话中提出要求，不要直接编辑文件；这样 AI 可以解释、审查并记录其选择。
- 这项能力仍受本机 Host/Origin/CSRF 防护约束，且不接受来自群聊原文的“写规则”指令。

### 每个群独立的事实记忆（不使用向量模型）

`rules.md` 保存的是管理员给机器人的全局行为规则；群长期记忆则是另一套按 `group_id` 严格隔离的、可追溯的事实账本。它用来记住群成员的稳定称呼/别名、明确偏好、人物关系、承诺与待办、长期背景事实，让机器人在同一个群里延续交往上下文，同时不会把 A 群的信息带到 B 群。

这套机制不需要 embedding 或向量压缩模型：

1. 最新约 50K 群聊原文仍完整交给主模型。模型只从本轮真实消息中提出结构化记忆候选，并附上原始 `message_id` 和逐字证据片段。
2. 候选会再经过独立核验；本地服务同时检查消息确实属于当前群、证据片段确实出现在原文中。证据不足、玩笑/反讽不明确、纯猜测或相互矛盾的候选不会直接成为有效事实。
3. 记忆按“主体 + 关系/属性 + 值 + 时态”结构保存。检索依赖当前群、人物标识、事实类型、关键词、时间和有效状态的精确/全文查询，而不是相似度向量。真正提交给 AI 时会连同证据和状态一起提供，模型不得把无证据记录说成已确认事实。
4. 新消息否定旧事实时不会悄悄覆盖数据库：系统新增更正版本，并把旧版本标为“已被更正”；已完成承诺、临时状态和主动撤回也有独立状态。这样既能更新认识，也能解释“为什么以前记得不一样”。
5. 每个群卡片中的“群长期记忆”可查看内容、置信/核验状态、更新时间和原消息证据。管理员可人工确认、更正、撤回或软删除。所谓删除只会停止 AI 使用该记录，旧版本及证据链仍留在本机审计中，避免下一轮又从旧聊天误学回来。

任何 LLM 都无法从理论上保证绝不遗忘或绝不误判；这里通过“逐条证据、二次核验、版本化冲突处理、人工纠错、按群隔离”把错误变成可发现、可解释、可修正的记录，而不是让一份不可检查的压缩摘要冒充记忆。原始事件仍保存在 SQLite，因此需要时还能回查来源。

## 测试

运行完整测试：

```powershell
.\.venv\Scripts\python.exe -m pytest
```

手动验收建议：

1. 启动 NapCat，扫码登录专用 QQ，确认控制台显示 OneBot 已连接。
2. 在测试群启用机器人，确认最新消息以原文形式进入模型上下文；检查一次最多抓取 200 条/50,000 字，且只有被挤出原文窗口的旧消息出现在历史摘要中。
3. 发送文字、图片、视频和可用群事件；确认新事件可触发回复，图片受视觉开关与容量限制约束，图片生成工具会先发送“正在生成图片，请等待至少5s”进度提示；对视频请求“看一下视频”，确认 ffmpeg 抽帧、300 KiB 分块视觉总结和最终合并结果进入主 Agent。
4. 测试模型先搜索/抓取/检索前文再发送消息，以及撤回消息；确认所有 QQ 操作只能作用于当前群，且只能撤回本应用发出的消息。
5. 在测试群明确说出一个长期偏好或承诺，确认“群长期记忆”出现带原消息 ID/引文的记录；再更正它，确认旧版本变为“已被更正”，新版本有效。另一个群不应看到这条记忆。
6. 停用群，继续在群内发消息；确认没有新的 API 请求，同时历史仍保留在本机。

## 登录后自动启动

安装当前 Windows 用户的计划任务（不需要管理员权限）：

```powershell
# 只启动本机服务
.\scripts\install-autostart.ps1

# 登录后先启动安装了 NapCat 的 QQNT，再启动本机服务
.\scripts\install-autostart.ps1 -NapCatExecutable 'C:\Path\To\QQ.exe'
```

该任务名为 `QQ 群 AI 代理`，触发条件是当前用户登录。它不保存 QQ 密码；如果 QQNT 尚未登录，仍需在 QQ 客户端自行扫码。移除计划任务：

```powershell
.\scripts\install-autostart.ps1 -Remove
```

任务只会启动本地 QQNT/NapCat 和本服务，仍请避免把控制台或 OneBot WebSocket 暴露到公网。
